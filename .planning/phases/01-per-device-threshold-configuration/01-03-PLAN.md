---
phase: 1
plan: 03
title: Simulator — Read Threshold from DB
wave: 2
depends_on:
  - 01-01
files_modified:
  - simulator/digital-twin/main.py
autonomous: true
requirements:
  - CFG-03
---

# Plan 01-03: Simulator — Read Threshold from DB

## Goal

Cập nhật `_fetch_active_devices()` để lấy thêm `min_value` và `max_value` từ DB. Lưu vào `DeviceInfo`. Cập nhật `_merged_profile()` để dùng DB threshold thay vì giá trị hardcode trong `profiles.json`, giúp Simulator tự động phản ánh ngưỡng farmer cấu hình.

## Context

- `DeviceInfo` dataclass hiện có: `device_id`, `feed_key`, `device_type`, `metric_type`, `is_active`
- `_fetch_active_devices()` query SQL tại dòng ~102 — cần thêm `d.min_value`, `d.max_value` vào SELECT
- `_merged_profile()` tại dòng ~75 set `merged.setdefault("min", 0)` và `merged.setdefault("max", 100)` hardcode — cần override bằng DB values khi có
- `_compute_value()` tại dòng ~136 dùng `vmin = float(profile.get("min", 0))` và `vmax = float(profile.get("max", 100))` — sẽ tự dùng DB values sau khi `_merged_profile()` được sửa
- `DeviceRuntime` dùng `profile` dict — không cần thay đổi `_compute_value()` nếu `profile["min"]`/`profile["max"]` được set đúng

## Tasks

### Task 1: Thêm min_value/max_value vào DeviceInfo dataclass

<read_first>
- `simulator/digital-twin/main.py` (lines 19-35: DeviceInfo và DeviceRuntime dataclasses)
</read_first>

<action>
Cập nhật `DeviceInfo` dataclass — thêm 2 fields sau `is_active`:

```python
@dataclass
class DeviceInfo:
    device_id: str
    feed_key: str
    device_type: str
    metric_type: str
    is_active: bool = False
    min_value: Optional[float] = None   # <-- thêm mới
    max_value: Optional[float] = None   # <-- thêm mới
```

`Optional` đã được import từ `typing` (dòng 12).
</action>

<acceptance_criteria>
- `DeviceInfo` dataclass có field `min_value: Optional[float] = None`
- `DeviceInfo` dataclass có field `max_value: Optional[float] = None`
- `Optional` import từ `typing` đã có sẵn (dòng 12: `from typing import Dict, Optional`)
</acceptance_criteria>

---

### Task 2: Cập nhật SQL query trong _fetch_active_devices()

<read_first>
- `simulator/digital-twin/main.py` (lines 102-125: _fetch_active_devices method)
</read_first>

<action>
Cập nhật SQL query trong `_fetch_active_devices()` để thêm `d.min_value` và `d.max_value`:

```python
def _fetch_active_devices(self) -> Dict[str, DeviceInfo]:
    sql = """
    SELECT d.id::text, d.adafruit_feed_key, m.device_type::text, m.metric_type::text,
           d.is_active, d.min_value, d.max_value
    FROM devices d
    JOIN models m ON m.id = d.model_id
    WHERE d.status = 'ACTIVE'
      AND d.adafruit_feed_key IS NOT NULL
      AND trim(d.adafruit_feed_key) <> ''
    """

    devices = {}
    with self._db_conn() as conn:
        with conn.cursor() as cur:
            cur.execute(sql)
            for row in cur.fetchall():
                info = DeviceInfo(
                    device_id=row[0],
                    feed_key=row[1].strip(),
                    device_type=row[2].upper(),
                    metric_type=row[3].upper(),
                    is_active=bool(row[4]),
                    min_value=float(row[5]) if row[5] is not None else None,  # <-- thêm
                    max_value=float(row[6]) if row[6] is not None else None,  # <-- thêm
                )
                devices[info.device_id] = info
    return devices
```
</action>

<acceptance_criteria>
- SQL SELECT bao gồm `d.min_value, d.max_value` (2 cột mới, index 5 và 6)
- `DeviceInfo` constructor truyền `min_value=float(row[5]) if row[5] is not None else None`
- `DeviceInfo` constructor truyền `max_value=float(row[6]) if row[6] is not None else None`
</acceptance_criteria>

---

### Task 3: Cập nhật _merged_profile() để dùng DB threshold

<read_first>
- `simulator/digital-twin/main.py` (lines 75-100: _merged_profile method)
</read_first>

<action>
Cập nhật `_merged_profile()` để override `min`/`max` từ DB khi farmer đã cấu hình ngưỡng:

```python
def _merged_profile(self, info: DeviceInfo, profiles: dict) -> dict:
    defaults = profiles.get("defaults", {})
    metrics = profiles.get("metrics", {})
    devices = profiles.get("devices", {})

    metric_profile = metrics.get(info.metric_type.upper(), {})
    device_profile = devices.get(info.device_id, {})
    feed_profile = devices.get(f"feed:{info.feed_key}", {})

    merged = {}
    merged.update(defaults)
    merged.update(metric_profile)
    merged.update(device_profile)
    merged.update(feed_profile)

    if info.device_type.upper() == "ACTUATOR" and "pattern" not in merged:
        merged["pattern"] = "actuator_state"

    merged.setdefault("interval_ms", 2000)
    merged.setdefault("pattern", "random_walk")
    merged.setdefault("min", 0)
    merged.setdefault("max", 100)
    merged.setdefault("step", 1.0)
    merged.setdefault("noise", 0.2)

    # Override min/max từ DB nếu farmer đã cấu hình ngưỡng
    if info.min_value is not None:
        merged["min"] = info.min_value
        self.logger.debug("Simulator: device=%s using DB min_value=%.2f", info.device_id, info.min_value)
    if info.max_value is not None:
        merged["max"] = info.max_value
        self.logger.debug("Simulator: device=%s using DB max_value=%.2f", info.device_id, info.max_value)

    return merged
```
</action>

<acceptance_criteria>
- `_merged_profile()` có block `if info.min_value is not None: merged["min"] = info.min_value`
- `_merged_profile()` có block `if info.max_value is not None: merged["max"] = info.max_value`
- Có `self.logger.debug(...)` log khi dùng DB threshold (không phải INFO để tránh noise)
- Fallback `merged.setdefault("min", 0)` và `merged.setdefault("max", 100)` vẫn giữ nguyên cho trường hợp chưa cấu hình
</acceptance_criteria>

---

## Verification

```bash
# Khởi động Simulator sau khi backend đã thêm cột min_value/max_value:
cd simulator/digital-twin
.venv/Scripts/python.exe main.py

# Kiểm tra log — nếu có device với min/max đã cấu hình, sẽ thấy:
# DEBUG [digital_twin] Simulator: device=<uuid> using DB min_value=20.00
# DEBUG [digital_twin] Simulator: device=<uuid> using DB max_value=90.00

# Test thủ công:
# 1. Cấu hình ngưỡng min=20, max=90 cho một cảm biến qua FE
# 2. Restart simulator
# 3. Observe: giá trị telemetry publish phải nằm trong [20, 90]
```

## Must Haves

- `DeviceInfo` có `min_value` và `max_value` (Optional[float], default None)
- SQL query lấy `d.min_value` và `d.max_value` từ DB
- `_merged_profile()` override `min`/`max` từ DB khi có giá trị
- Simulator publish giá trị trong khoảng [min_value, max_value] khi farmer đã cấu hình
- Log DEBUG khi dùng DB threshold (không log INFO để tránh noise)
