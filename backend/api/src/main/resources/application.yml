spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yolofarm_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:23012005}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        "format_sql": true
  jackson:
    property-naming-strategy: SNAKE_CASE

jwt:
  secret-key: ${JWT_SECRET_KEY:fklasdjflkkasdjflkasdfjasldkkkfjasdlfjasdfasdf}
  expiration: 86400000

adafruit:
  mqtt:
    broker-url: ${ADAFRUIT_BROKER_URL:ssl://io.adafruit.com:8883}
    client-id: "YoloFarm_${random.int[100,999]}"
    username: ${ADAFRUIT_USERNAME:HaiXon}
    password: ${ADAFRUIT_IO_KEY:aio_RVDQ11UkhyvgBLeqFN2SA9fzBKxb}

app:
  websocket:
    allowed-origins: ${WS_ALLOWED_ORIGINS:http://localhost:3000}
